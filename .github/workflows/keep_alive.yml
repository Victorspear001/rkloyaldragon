name: Keep Supabase Alive

on:
  schedule:
    # Run at 6:00 AM and 6:00 PM UTC every day
    - cron: '0 6,18 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel API
        run: |
          # Replace with your ACTUAL Vercel URL
          curl -s https://rkloyaldragon.vercel.app/api/customer > /dev/null
